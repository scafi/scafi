dependencies {
    compile project(':scafi-commons')
}

sourceSets {
    main {
        scala {
            srcDirs = ['src/main/scala', 'src/main/scala'+scalaSuffix.replace("_","-")]
        }
    }
}